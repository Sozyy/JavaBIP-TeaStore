# Chips_to_JavaBIP

Tool to transform a CHIPS model in the form of an XMI file into a JavaBIP project.

## Usage

To generate a JavaBIP project for the given example, just run 'make all' followed by 'make chips_to_javabip' and the generated Java files will be in the 'generated_javabip' folder.